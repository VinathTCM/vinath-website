# 部署到真实服务器前，复制这个文件为 .env（或者直接在部署平台的"环境变量"设置界面里配置这几项），
# 并把下面的示例值换成真实值。.env 本身不要提交进代码仓库或者分享出去。

# 【必须设置】JWT签名密钥——用来给管理员登录状态签名。不设置的话，生产环境下服务会直接拒绝启动
# （开发环境会用一个不安全的默认值继续跑，但会打印警告）。
# 生成一个随机密钥，在这台电脑或者部署平台的终端里运行：
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=在这里粘贴上面命令生成的随机字符串

# 【可选】服务监听的端口。大部分托管平台（Railway/Render等）会自动注入这个变量，不用手动设置。
PORT=3001

# 【建议设置】生产环境标记。设置成 production 后，JWT_SECRET 校验会变严格
# （检测到不安全的默认密钥时直接拒绝启动，而不只是打印警告）。
NODE_ENV=production
